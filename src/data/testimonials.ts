/**
 * Testimonials.
 *
 * Attribution is mandatory: real name, role and company. Anonymous praise
 * ("Hospitality Client", "F&B Brand") is not credible and cannot be
 * represented by the type.
 *
 * This list is intentionally empty until quotes are collected and approved.
 * While it is empty the testimonials section does not render at all — the
 * home page falls back to project evidence and named relationships, which is
 * stronger than invented praise.
 */

import type { Testimonial } from "@/types/portfolio";

export const testimonials: Testimonial[] = [];
